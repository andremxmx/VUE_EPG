import type { Ref } from 'vue';
import type { BaseTimeFormat, ProgramItem } from '../helpers/types';
interface useProgramProps<T> {
    program: Ref<T>;
    isBaseTimeFormat: Ref<BaseTimeFormat>;
    minWidth?: number;
}
export declare function useProgram<T extends ProgramItem>({ program, isBaseTimeFormat, minWidth, }: useProgramProps<T>): {
    format12HoursTime: (date: string | number | Date) => string;
    isLive: Ref<boolean>;
    isMinWidth: boolean;
    styles: import("vue").ComputedRef<{
        width: number;
        position: {
            width: string;
            height: string;
            top: string;
            left: string;
        };
    }>;
};
export {};
